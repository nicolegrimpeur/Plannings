import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'plannings.ionic.all',
  appName: 'All Plannings',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
